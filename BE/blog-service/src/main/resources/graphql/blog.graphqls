type Query {
    blogById(id: String): BlogDto
}

type BlogDto {
    id: String
    active: String
    createdAt: String
    updatedAt: String
    createdBy: String
    updatedBy: String
    userId: String
    title: String
    body: String
}