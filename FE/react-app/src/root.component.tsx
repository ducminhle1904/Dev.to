import React from "react";
import App from "./App";

export default function Root(props) {
    return (
        <section style={{ marginTop: 50 }}>
            <App name="Testapp" />
        </section>
    );
}
